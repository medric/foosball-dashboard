import React from 'react';
import { Route } from 'react-router-dom';

const ParticipantsList = ({ match }) => {
    return (
        <div className="participants-list">
            <h2>Participants</h2>}
        </div>
    )   
}

export default ParticipantsList;